[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "acquisition_collect"
version = "0.1.0"
description = "Cross-platform targeted forensic triage collector"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Mostafa Imam" }]
keywords = ["dfir", "forensics", "triage", "acquisition", "incident-response"]
classifiers = [
    "Environment :: Console",
    "Intended Audience :: Information Technology",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Topic :: Security",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/mostafaimam/Forensics-Tools/tree/main/acquisition/acquisition_collect"
Issues = "https://github.com/mostafaimam/Forensics-Tools/issues"

[project.scripts]
acquisition_collect = "acquisition_collect.cli:main"

[tool.setuptools]
packages = ["acquisition_collect", "acquisition_collect.readers"]

[tool.setuptools.package-data]
acquisition_collect = ["targets/**/*.toml"]

[tool.pytest.ini_options]
testpaths = ["tests"]
